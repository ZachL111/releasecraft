Gem::Specification.new do |spec|
  spec.name = "releasecraft"
  spec.version = "0.1.0"
  spec.summary = "Classify changelog fragments into deterministic release sections."
  spec.files = Dir["lib/**/*.rb"]
  spec.required_ruby_version = ">= 3.0"
end
